# Integrações (importação para o banco)

Documentos que dizem a um robô (crawler + IA) onde gravar cada campo de um payload no banco do Kizuna e em que ordem.

| Categoria | Documento | Fonte |
|---|---|---|
| Cinema | [cinema-depara.md](cinema-depara.md) | ingresso.com |

## Manter atualizado

- Mudou coluna de `services`, `service_addresses`, seed de formulário, taxonomia ou migration que o de-para cita: atualize o documento no mesmo trabalho.
- Categoria nova: use a skill `criar-depara` (`.claude/skills/criar-depara/SKILL.md`) e adicione a linha na tabela acima.
- O `cinema-depara.md` é o modelo de estrutura para os próximos.
